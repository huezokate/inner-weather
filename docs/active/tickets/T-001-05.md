---
id: T-001-05
story: S-001
title: insforge-ai-intensity-classifier
type: feature
status: open
priority: medium
phase: done
depends_on: [T-001-03]
---

## Context

Build Plan Task 5. Replace source-prior intensities for live items with real model scores from
the InsForge AI gateway — demonstrating InsForge twice (DB + AI gateway). Hero set stays
hand-tagged.

Depends on T-001-03 because it refines the live items produced by `fetchLiveFeed()` (Reddit +
You.com) and edits `feed.ts` — serialized behind the live-feed work.

✅ InsForge keys are NOW FILLED and verified working, so this runs LIVE. You may still need
`cli ai setup` / an OpenRouter model configured on the InsForge AI gateway — if the gateway
isn't set up or a call fails, fall back to the existing source-prior intensities (no model
call) and note it. Never let a classifier failure break the demo.

## Implementation notes

- New file `src/lib/classify.ts`. Send live item titles (Reddit + You.com) to the InsForge AI
  gateway with a prompt like: "Rate 1–5 how activating/agitating this content is. Return JSON
  `[{id,intensity}]`." Parse defensively; wrap in try/catch.
- In `fetchLiveFeed()`: after gathering live items, call the classifier and overwrite each live
  item's `intensity` with the model score. Keep HERO_FEED intensities untouched.
- On any failure or missing keys: keep the source-prior intensities unchanged.

## Acceptance Criteria

- [ ] `src/lib/classify.ts` exists; live items get sensible 1–5 model scores when keys present.
- [ ] Hero set intensities remain hand-tagged/unchanged.
- [ ] If the AI gateway isn't configured or a call fails, live items keep their source-prior
      intensities and the skip is noted; build passes.
- [ ] Reddit + You.com lanes still populate; HERO_FEED + flip still work.
- [ ] `npx tsc -b` is clean and the dev server boots.
