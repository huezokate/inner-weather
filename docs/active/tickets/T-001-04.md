---
id: T-001-04
story: S-001
title: insforge-diary-backend
type: feature
status: open
priority: medium
phase: done
depends_on: [T-001-01]
---

## Context

Build Plan Task 4. Persist readiness over time — the NASI "Memory" pillar and the $500 prize
integration. On each settled score, write a `diary` row to InsForge; show a small "weather
diary" strip of recent readings.

Depends on T-001-01 because both edit `App.tsx` — serialized to avoid conflicting edits.

✅ InsForge keys are NOW FILLED and verified working: base URL
`https://gkeqi24f.us-east.insforge.app` reachable (`/api/health` → 200), `VITE_INSFORGE_ANON_KEY`
authenticates client-side, admin `INSFORGE_API_KEY` (`ik_…`) authenticates. **Run this LIVE** —
provision the `diary` table and write real rows. The `diary` table does NOT exist yet (table
list is empty), so creating it is part of this ticket. Keep a **localStorage** fallback only as
a runtime safety net if a call fails — do not let a failed write break the demo. Project-id:
`7545b6ea-8d51-4892-aadb-10c1dd24b885`.

## Implementation notes

- `npm i @insforge/sdk`. New file `src/lib/insforge.ts` → `createClient({ baseUrl, anonKey })`
  reading the two env vars; export a guarded helper that no-ops (or uses localStorage) when
  they're absent.
- Provision via the InsForge MCP/CLI (admin `INSFORGE_API_KEY`): table `diary`
  (`id`, `created_at`, `readiness int`, `tier text`, `shielded_count int`, `overrides jsonb`).
  Add RLS allowing the anon/auth role to insert + select (RLS is the usual gotcha — the anon
  key is what the client uses).
- In `App.tsx`: on score settle (debounced), insert a `diary` row (or localStorage entry).
- Add a small "weather diary" strip showing readiness across recent entries.
- Optional auth (`auth.signInWithPassword`) for a per-user diary — only if keys present.

## Acceptance Criteria

- [ ] `src/lib/insforge.ts` exists with a client guarded for missing env vars.
- [ ] The `diary` table is provisioned in InsForge with insert+select RLS for the anon role.
- [ ] Changing readiness writes a real `diary` row to InsForge.
- [ ] The diary strip reads recent entries back from InsForge and renders them.
- [ ] A failed write/read falls back to localStorage so the demo never breaks.
- [ ] Oura readiness from T-001-01 still works; HERO_FEED + flip still work.
- [ ] `npx tsc -b` is clean and the dev server boots.
