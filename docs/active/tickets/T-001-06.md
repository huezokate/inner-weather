---
id: T-001-06
story: S-001
title: polish-and-demo
type: feature
status: open
priority: high
phase: done
depends_on: [T-001-04, T-001-05]
---

## Context

Build Plan Task 6. Final polish for the hackathon demo. Runs last, after both tracks land
(InsForge/diary and the classified live feed), and touches `App.tsx` + `feed.ts`, so it must
follow T-001-04 and T-001-05.

## Implementation notes

- Tune the Sharp↔Fog flip timing so shielded cards visibly un-blur and reorder — that's the
  wow. Verify the rain/fog/glow morph reads on a projector (high contrast).
- Craft one *beautiful* Fog state and one *beautiful* Sharp state using the hero set.
- Record a GIF of the flip with the browser `gif_creator` tool, named `inner-weather-flip.gif`.
- Draft the ≤3-min demo script in `../INNER_WEATHER_CONCEPT.md` §1 wording.
- Deploy: `npm run build` → Netlify / GitHub Pages (match Kate's other projects). Move secrets
  to a proxy/serverless function before any public deploy; do not ship API keys in the bundle.

## Acceptance Criteria

- [ ] The Sharp↔Fog flip is visibly smooth — cards un-blur and reorder, high-contrast on a
      projector.
- [ ] One polished Fog state and one polished Sharp state with the hero set.
- [ ] `inner-weather-flip.gif` recorded.
- [ ] ≤3-min demo script drafted in `../INNER_WEATHER_CONCEPT.md` §1.
- [ ] `npm run build` succeeds; deploy steps documented (secrets moved off-client before any
      public deploy).
- [ ] `npx tsc -b` is clean and the dev server boots.
